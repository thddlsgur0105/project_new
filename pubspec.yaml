name: flutter_application_start
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.2.0
  http: ^1.4.0
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  fl_chart: ^0.68.0
  web_socket_channel: ^3.0.3
    
  device_frame: ^1.4.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/
  uses-material-design: true